describe('Sum of three numbers', () => {
  test('Sum of 2, 4, and 6', () => {
    let result = 2 + 4 + 6
    expect(result).toBe(12)
  })
})

describe('Sum of two numbers', () => {
  test('Sum of 1 and 6', () => {
    let result = 1 + 6
    expect(result).toBe(12)
  })
})